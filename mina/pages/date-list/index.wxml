<!--pages/date-list/index.wxml-->
<view class='date-wrap'>
  <date-item-cmp wx:for="{{dateList}}" 
                wx:key="{{item.date}}" 
                class="date"
                item="{{item}}">
  </date-item-cmp>
</view>
