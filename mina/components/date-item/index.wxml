<!--components/date-item/index.wxml-->
<view class='{{ can_order ? "item-wrap": "item-wrap disable" }}'
      bindtap="click"
      data-item="{{ item }}">
  <p>
  {{ date }}
  </p>
  <p>
  {{ week}} 
  </p>
</view>
